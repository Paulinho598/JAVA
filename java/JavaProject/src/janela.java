import javax.swing.JFrame;

class Window extends JFrame{
    public Window(){
        setResizable(false);
        setSize(800,500);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setVisible(true);
    }
}